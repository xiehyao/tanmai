# 复制本文件为同目录下的 .env 并填写真实值（.env 已加入 .gitignore，勿提交）
# 微信小程序后台 → 开发 → 开发管理 → 开发设置 → AppID 与 AppSecret

WECHAT_APPID=
WECHAT_SECRET=

# 可选：数据库等（不配则使用 config 默认值）
# DATABASE_URL=mysql+pymysql://user:pass@localhost:3306/tanmai
