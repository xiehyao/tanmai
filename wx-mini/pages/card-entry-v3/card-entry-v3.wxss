/* pages/card-entry-v3/card-entry-v3.wxss */
/* 模仿「名片全能王」编辑资料样式 */

.v3-page {
  min-height: 100vh;
  background: #f5f5f5;
  padding-bottom: env(safe-area-inset-bottom);
}

/* 步骤条（1. 个人信息 / 2. 校友会参与意愿） */
.v3-step-bar {
  display: flex;
  background: #fff;
  padding: 24rpx;
  gap: 24rpx;
  border-bottom: 1rpx solid #eee;
}
.v3-step-item {
  flex: 1;
  text-align: center;
  font-size: 28rpx;
  color: #999;
  padding: 16rpx 0;
}
.v3-step-item.active {
  color: #667eea;
  font-weight: 600;
  border-bottom: 4rpx solid #667eea;
}
.v3-step2-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
  padding: 24rpx 24rpx 8rpx 24rpx;
  background: #f5f5f5;
}

.v3-content {
  height: calc(100vh - 140rpx);
  padding-bottom: 24rpx;
}

/* 区块 */
.section {
  margin: 24rpx;
  padding: 28rpx 24rpx;
  background: #fff;
  border-radius: 16rpx;
}

.section-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 24rpx;
}

/* 卡片内子部分标题（如 对校友会需求 / 为校友会服务意愿） */
.subsection-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #555;
  margin: 24rpx 0 16rpx 0;
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
}

.section-header .section-title {
  margin-bottom: 0;
}

.section-action {
  font-size: 28rpx;
  color: #667eea;
}

/* 名片样式占位 */
.card-style-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.card-style-placeholder {
  flex: 1;
  height: 120rpx;
  background: #f8f8f8;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  padding: 0 24rpx;
}

.card-style-lines {
  width: 100%;
  height: 60rpx;
  background: linear-gradient(to bottom, transparent 0%, transparent 18rpx, #e0e0e0 18rpx, #e0e0e0 22rpx, transparent 22rpx),
    linear-gradient(to bottom, transparent 0%, transparent 38rpx, #e0e0e0 38rpx, #e0e0e0 42rpx, transparent 42rpx),
    linear-gradient(to bottom, transparent 0%, transparent 58rpx, #e0e0e0 58rpx, #e0e0e0 62rpx, transparent 62rpx);
  background-size: 100% 100%;
}

/* 添加按钮 */
.add-btn {
  display: flex;
  align-items: center;
  font-size: 26rpx;
  color: #667eea;
}

.add-icon {
  width: 40rpx;
  height: 40rpx;
  line-height: 40rpx;
  text-align: center;
  font-size: 32rpx;
  font-weight: 600;
  background: #667eea;
  color: #fff;
  border-radius: 50%;
  margin-right: 8rpx;
}

.add-icon-sm {
  width: 48rpx;
  height: 48rpx;
  line-height: 48rpx;
  text-align: center;
  font-size: 32rpx;
  font-weight: 600;
  color: #667eea;
  background: rgba(102, 126, 234, 0.1);
  border-radius: 50%;
}

/* 表单项 */
.form-row {
  display: flex;
  align-items: center;
  padding: 24rpx 0;
  border-bottom: 1rpx solid #eee;
}

.form-row:last-child {
  border-bottom: none;
}

/* 可见性图标（子项最右侧，在 [>] / [+] 等之后，可点击） */
.visibility-cell {
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-left: 12rpx;
}
/* 隐私状态用 emoji 展示：👁️公开 🔒私密 🎭打码 🎓校友 👥好友，缩小+灰化不抢眼 */
.visibility-icon {
  font-size: 24rpx;
  padding: 2rpx 6rpx;
  opacity: 0.65;
  background: rgba(102, 126, 234, 0.06);
  border-radius: 6rpx;
}
.visibility-option-row {
  display: flex;
  align-items: center;
  padding: 28rpx 24rpx;
  border-bottom: 1rpx solid #eee;
}
.visibility-option-row.visibility-option-selected {
  background: rgba(102, 126, 234, 0.12);
}
.visibility-option-icon {
  font-size: 32rpx;
  margin-right: 16rpx;
}
.visibility-option-label {
  font-size: 28rpx;
  color: #333;
}

.form-label {
  width: 160rpx;
  flex-shrink: 0;
  font-size: 28rpx;
  color: #333;
}

.form-input {
  flex: 1;
  font-size: 28rpx;
  color: #333;
}

.form-input.multi-line {
  height: 72rpx;  /* 固定约两行高度，内容超出可滚动 */
}

.form-picker {
  flex: 1;
}

.form-avatar {
  width: 80rpx;
  height: 80rpx;
  border-radius: 8rpx;
  background: #eee;
}

.form-input-placeholder {
  color: #999;
  font-size: 28rpx;
}

/* 头像选择半屏网格 */
.avatar-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 24rpx;
  padding: 24rpx 0;
}
.avatar-grid-item {
  width: 120rpx;
  height: 120rpx;
  border-radius: 12rpx;
  overflow: hidden;
  background: #f0f0f0;
}
.avatar-grid-img {
  width: 100%;
  height: 100%;
}

/* 名片预览（参考名片夹） */
.card-preview-block {
  padding: 24rpx 0;
  background: #f8f8f8;
  border-radius: 16rpx;
}
.card-preview-row {
  display: flex;
  padding: 0 24rpx;
}
.card-preview-avatar {
  width: 160rpx;
  height: 160rpx;
  border-radius: 16rpx;
  margin-right: 24rpx;
  background: #e5e7eb;
  flex-shrink: 0;
}
.card-preview-main {
  flex: 1;
  min-width: 0;
}
.card-preview-name {
  display: block;
  font-size: 32rpx;
  font-weight: 700;
  color: #111;
  margin-bottom: 8rpx;
}
.card-preview-company {
  display: block;
  font-size: 26rpx;
  color: #666;
  margin-bottom: 8rpx;
}
.card-preview-meta {
  display: block;
  font-size: 24rpx;
  color: #888;
  margin-bottom: 4rpx;
}

/* 名片预览（参考名片夹） */
.card-preview-block {
  padding: 24rpx 0;
  background: #f8f8f8;
  border-radius: 16rpx;
}
.card-preview-row {
  display: flex;
  padding: 0 24rpx;
}
.card-preview-avatar {
  width: 160rpx;
  height: 160rpx;
  border-radius: 16rpx;
  margin-right: 24rpx;
  background: #e5e7eb;
  flex-shrink: 0;
}
.card-preview-main {
  flex: 1;
  min-width: 0;
}
.card-preview-name {
  display: block;
  font-size: 32rpx;
  font-weight: 700;
  color: #111;
  margin-bottom: 8rpx;
}
.card-preview-company {
  display: block;
  font-size: 26rpx;
  color: #666;
  margin-bottom: 8rpx;
}
.card-preview-meta {
  display: block;
  font-size: 24rpx;
  color: #888;
  margin-bottom: 4rpx;
}

.form-arrow {
  margin-left: 16rpx;
  font-size: 28rpx;
  color: #999;
}

.form-badges {
  flex: 1;
  display: flex;
  gap: 16rpx;
  flex-wrap: wrap;
}

.badge {
  padding: 6rpx 16rpx;
  font-size: 22rpx;
  border-radius: 8rpx;
}

.badge.grey {
  background: #f0f0f0;
  color: #666;
}

.badge.teal {
  background: #e6f7f7;
  color: #00a6a6;
}

/* 添加链接 */
.add-link {
  font-size: 28rpx;
  color: #667eea;
  text-align: center;
  padding: 24rpx 0;
}

.section-hint {
  display: block;
  font-size: 24rpx;
  color: #888;
  line-height: 1.5;
  margin-top: 8rpx;
}

.section-subtitle {
  font-size: 28rpx;
  color: #666;
  margin: 24rpx 0 16rpx 0;
}

/* 多选选项 */
.checkbox-wrap {
  flex: 1;
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx 24rpx;
  padding: 16rpx 0;
}

.checkbox-item {
  display: flex;
  align-items: center;
  font-size: 26rpx;
  color: #333;
}

.checkbox-item checkbox {
  margin-right: 8rpx;
}

/* 对校友会需求分组 */
.form-row-needs {
  align-items: flex-start;
}
.form-row-needs .form-label {
  padding-top: 16rpx;
}
.needs-groups {
  flex: 1;
}
.needs-group {
  margin-bottom: 24rpx;
}
.needs-group:last-child {
  margin-bottom: 0;
}
.needs-group-title {
  font-size: 26rpx;
  color: #667eea;
  font-weight: 500;
  margin-bottom: 12rpx;
}
.needs-group .checkbox-wrap {
  padding: 0 0 0 0;
}

/* 结构化资源 */
.resource-item {
  margin-top: 24rpx;
  padding: 24rpx;
  background: #f8f8f8;
  border-radius: 12rpx;
}

.resource-item-head {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
  font-size: 28rpx;
  color: #333;
}

.resource-remove {
  font-size: 26rpx;
  color: #e74c3c;
}

/* 工作/教育经历 */
.experience-item {
  display: flex;
  align-items: flex-start;
  padding: 24rpx 0;
  border-bottom: 1rpx solid #eee;
}

.experience-item:last-child {
  border-bottom: none;
}

.exp-icon {
  width: 64rpx;
  height: 64rpx;
  line-height: 64rpx;
  text-align: center;
  font-size: 36rpx;
  background: #f0f0f0;
  border-radius: 12rpx;
  flex-shrink: 0;
  margin-right: 20rpx;
}

.exp-content {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.exp-primary {
  font-size: 30rpx;
  font-weight: 600;
  color: #333;
}

.exp-secondary {
  font-size: 26rpx;
  color: #333;
}

.exp-tertiary {
  font-size: 24rpx;
  color: #666;
}

/* 纸质名片 */
.paper-cards {
  display: flex;
  gap: 20rpx;
  white-space: nowrap;
  padding: 16rpx 0;
}

.paper-card {
  position: relative;
  flex-shrink: 0;
  width: 180rpx;
  height: 110rpx;
}

.paper-card-img {
  width: 100%;
  height: 100%;
  border-radius: 12rpx;
  background: #eee;
}

.paper-card-del {
  position: absolute;
  top: -8rpx;
  right: -8rpx;
  width: 40rpx;
  height: 40rpx;
  line-height: 40rpx;
  text-align: center;
  background: rgba(0,0,0,0.6);
  color: #fff;
  border-radius: 50%;
  font-size: 28rpx;
}

.paper-card-add {
  flex-shrink: 0;
  width: 180rpx;
  height: 110rpx;
  line-height: 110rpx;
  text-align: center;
  font-size: 48rpx;
  color: #999;
  background: #f5f5f5;
  border: 2rpx dashed #ddd;
  border-radius: 12rpx;
}

.bottom-spacer {
  height: 40rpx;
}

/* 底部按钮 */
.v3-footer {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  align-items: center;
  gap: 24rpx;
  padding: 20rpx 24rpx;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
  background: #fff;
  box-shadow: 0 -2rpx 12rpx rgba(0,0,0,0.06);
}

.btn-preview {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 16rpx 32rpx;
  background: #fff;
  border: 1rpx solid #ddd;
  border-radius: 12rpx;
  font-size: 24rpx;
  color: #666;
}

.btn-preview-icon {
  font-size: 36rpx;
  margin-bottom: 4rpx;
}

.btn-save {
  flex: 1;
  height: 80rpx;
  line-height: 80rpx;
  font-size: 30rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: #fff;
  border: none;
  border-radius: 12rpx;
}

.btn-save::after {
  border: none;
}

/* 半屏弹层 */
.sheet-mask {
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  background: rgba(0,0,0,0.4);
  z-index: 900;
}

.sheet-panel {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  background: #fff;
  border-radius: 24rpx 24rpx 0 0;
  z-index: 901;
  max-height: 75vh;
  transform: translateY(100%);
  transition: transform 0.3s;
}

.sheet-panel.sheet-show {
  transform: translateY(0);
}

.sheet-head {
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  padding: 28rpx 80rpx;
  border-bottom: 1rpx solid #eee;
}

.sheet-close {
  position: absolute;
  left: 24rpx;
  font-size: 44rpx;
  color: #999;
}

.sheet-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
}

.sheet-body {
  max-height: 60vh;
  padding: 0 24rpx 32rpx;
}

.sheet-actions {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 32rpx;
  padding-top: 24rpx;
}

.sheet-btn-delete {
  font-size: 28rpx;
  color: #f44;
}

.sheet-btn-save {
  flex: 1;
  max-width: 400rpx;
  margin-left: 24rpx;
  height: 80rpx;
  line-height: 80rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: #fff;
  border: none;
  border-radius: 12rpx;
  font-size: 30rpx;
}

.sheet-btn-save::after {
  border: none;
}

/* 添加联系方式 - 网格 */
.contact-sheet .contact-type-grid {
  display: flex;
  flex-wrap: wrap;
  padding: 24rpx 16rpx 48rpx;
}

.contact-type-item {
  width: 25%;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 24rpx 8rpx;
}

.contact-type-icon {
  width: 88rpx;
  height: 88rpx;
  line-height: 88rpx;
  text-align: center;
  font-size: 40rpx;
  background: #f5f5f5;
  border-radius: 50%;
  margin-bottom: 12rpx;
}

.contact-type-label {
  font-size: 24rpx;
  color: #333;
}

/* 联系方式行 - 删除按钮 */
.contact-row {
  position: relative;
}

.contact-remove {
  width: 48rpx;
  height: 48rpx;
  line-height: 48rpx;
  text-align: center;
  font-size: 36rpx;
  color: #f44;
  margin-left: 12rpx;
}

.contact-add-address {
  width: 48rpx;
  height: 48rpx;
  line-height: 48rpx;
  text-align: center;
  font-size: 32rpx;
  font-weight: 600;
  color: #667eea;
  background: rgba(102, 126, 234, 0.1);
  border-radius: 50%;
  margin-left: 12rpx;
}

.form-row-with-switch {
  flex-wrap: wrap;
}

.work-current-wrap {
  display: flex;
  align-items: center;
  margin-left: 16rpx;
}

.work-current-switch {
  transform: scale(0.8);
}

.work-current-label {
  font-size: 26rpx;
  color: #666;
  margin-left: 8rpx;
}

/* 校董会职务半屏选项平铺 */
.board-option-row {
  padding: 28rpx 24rpx;
  border-bottom: 1rpx solid #eee;
}
.board-option-label {
  font-size: 28rpx;
  color: #333;
  line-height: 1.4;
}

/* 个人介绍：横向滚动简介卡片（参考校友简介卡） */
.intro-cards-scroll {
  white-space: nowrap;
  padding: 24rpx 0;
  margin: 0 -24rpx;
}
.intro-card {
  display: inline-block;
  width: 280rpx;
  margin-left: 24rpx;
  padding: 24rpx;
  background: #f8f8f8;
  border-radius: 16rpx;
  vertical-align: top;
  box-sizing: border-box;
}
.intro-card:first-child {
  margin-left: 24rpx;
}
.intro-card-photo {
  width: 100%;
  height: 200rpx;
  border-radius: 12rpx;
  background: #eee;
  display: block;
  margin-bottom: 16rpx;
}
.intro-card-name {
  display: block;
  font-size: 30rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 8rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.intro-card-desc {
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  font-size: 24rpx;
  color: #666;
  overflow: hidden;
  line-height: 1.4;
}
.intro-card-scene {
  display: block;
  font-size: 22rpx;
  color: #999;
  margin-top: 8rpx;
}
.intro-card-add {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  height: 280rpx;
  font-size: 48rpx;
  color: #667eea;
  background: rgba(102, 126, 234, 0.08);
  border: 2rpx dashed #667eea;
}
